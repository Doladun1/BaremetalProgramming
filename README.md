Fun Projects to build on FRDM-KL25Z Board

Contains Driver folder for each peripherial on the board ( I2C, LED, accelerometer, etc. ) 

Contains sample projects to run on your own in /src folder

Compile the src files and dependencies to build .srec then drag and drop to FRDM board

NOte: projects were built on ubuntu , Makefile is included
